services:
  docs:
    image: school_diary_local_docs
    container_name: school_diary_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./school_diary:/app/school_diary:z
    ports:
      - '9000:9000'
    command: /start-docs
